# Session 2026-02-23 #2 — Sprint 0 구현

## Summary

모노레포 마이그레이션 (Sprint 0) 완료.

## What was done

1. **루트 인프라**: pnpm-workspace.yaml, turbo.json, 루트 package.json 생성
2. **tooling/**: typescript (base, nextjs, react-library), eslint, prettier 공유 설정
3. **packages/widget-core**: WidgetMeta, WidgetDefinition 타입 + registry (Map)
4. **packages/widgets**: flip-clock, life-progress 위젯 추출 (기존 page.tsx → 컴포넌트 분리)
5. **packages/ui**: stub (빈 export)
6. **apps/web**: Next.js 앱 재구성
   - embed/[widgetId] 동적 라우트 (generateStaticParams + WidgetRenderer)
   - notion/time, notion/life-progress 하위호환 라우트
   - transpilePackages 설정
   - globals.css에 @source 디렉티브 추가 (Tailwind v4 packages/ 스캔)
7. **정리**: 구 파일 삭제, .gitignore 업데이트, deploy.yml (npm→pnpm)

## Build Result

```
Route (app)                                 Size  First Load JS
┌ ○ /                                      127 B         102 kB
├ ● /embed/[widgetId]                    2.16 kB         104 kB
├   ├ /embed/flip-clock
├   └ /embed/life-progress
├ ○ /notion/life-progress                 1.2 kB         103 kB
└ ○ /notion/time                         1.11 kB         103 kB
```

## Issues & Fixes

- widget-core에 `@types/react` devDep 누락 → 추가로 해결
- 백그라운드 Bash 에이전트 권한 이슈 → 직접 Write 도구로 파일 생성

## Next Steps

- Sprint 1: 쿼리 파라미터 시스템 (Zod 스키마, useWidgetParams 훅)
- dev 서버 수동 테스트 권장 (`pnpm dev`)
